services:
  money-graph:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data:ro
      - ./out:/app/out
    command: >
      sh -c "python3 -m pipeline --data data --out out &&
      python3 -m uvicorn api.main:app --host 0.0.0.0 --port 8000"
